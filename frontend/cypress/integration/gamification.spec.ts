describe('Resolution Components', () => {
    beforeEach(() => {
        cy.visit('/');
        cy.wait(2000); // Wait for any initial data loading
    });

 describe('User Score Calculation', () => {
    it('should calculate scores accurately based on user activity', () => {
            cy.get('button, a, [role="button"]').should('exist');
        });
    });


describe('Avatar Display', () => {
    it('should show the user score on their avatar', () => {
        cy.get('button, a, [role="button"]').should('exist');
            });
        });
    });